-- Programs used throughout the rest of the config (see keybindings.lua).
-- Migrated from defaults.conf ($terminal, $alt_terminal, ...).
return {
	terminal = "ghostty",
	alt_terminal = "alacritty",
	file_manager = "nautilus",
	-- menu = "rofi -show drun",
	menu = "walker",
	search_browser = "chromium",
}
